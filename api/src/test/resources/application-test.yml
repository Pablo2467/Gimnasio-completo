spring:
  datasource:
    url: jdbc:h2:mem:gymflow_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  flyway:
    enabled: false   

jwt:
  secret: "CLAVE_DE_PRUEBA_SOLO_PARA_TESTS_DE_AL_MENOS_32_CARACTERES"
  access-token-expiration-ms: 900000
  refresh-token-expiration-ms: 604800000